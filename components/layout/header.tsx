import React from 'react';

import { View, Logo } from 'vcc-ui';

const Header = () => {
  return (
    <View margin={10}>
      <Logo type="spreadmark" height={24} />
    </View>
  );
};

export default Header;
